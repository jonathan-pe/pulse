# Marketing app — quick start

This directory runs a small marketing site for Pulse.

Run locally:

```bash
cd apps/marketing
pnpm install
pnpm dev
```

The "Open App" button on the home page points to the local web app at `http://localhost:5173` — update this in `src/App.tsx` for production.
