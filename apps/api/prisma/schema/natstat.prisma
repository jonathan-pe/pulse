// natstat.prisma — NatStat provider metadata tables

model NatStatTeam {
  id        String   @id // NatStat team ID (e.g., "2022531")
  code      String // Team code (e.g., "JAX")
  name      String // Team name (e.g., "Jacksonville Jaguars")
  league    String // League (e.g., "NFL")
  active    Boolean  @default(true)
  badgeUrl  String? // Small light-theme logo (ESPN default mark), mirrors Team.logoUrl
  logoUrl   String? // Small dark-theme logo (ESPN dark mark), mirrors Team.logoUrlDark
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([league, code])
  @@index([league, active])
}
