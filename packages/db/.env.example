# Local PostgreSQL (via Docker Compose)
DATABASE_URL="postgresql://pulse:pulse_dev_password@localhost:5432/pulse_dev"

# Optional: Only needed for connection pooling (e.g., Supabase, PgBouncer)
# For local dev, leave this commented out. When migrating to Supabase:
# DIRECT_URL="postgresql://postgres:password@db.xxx.supabase.co:5432/postgres"