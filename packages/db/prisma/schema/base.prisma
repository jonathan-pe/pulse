generator client {
  provider = "prisma-client"
  output   = "../../generated/prisma"
}

datasource db {
  provider = "postgresql"
  // Note: In Prisma v7, the connection URL is configured in prisma.config.ts
  // via the datasource.url field instead of here
}
